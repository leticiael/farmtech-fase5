# Dependencias do Ir Alem, separadas das do notebook da raiz.
# Versoes fixadas nas quais os dois scripts foram executados.

# treinar_classificador.py
scikit-learn==1.5.2
# scipy fixada de proposito: sklearn 1.5.2 declara scipy>=1.6.0 sem teto, e
# com scipy 1.18.0 o LogisticRegression emite um OptimizeWarning que vem de
# dentro do sklearn, nao deste projeto. Fixar aqui torna o aviso reproduzivel
# em vez de depender do que o resolvedor escolher.
scipy==1.18.0
pandas==2.2.3
numpy==2.1.3
joblib==1.4.2

# assinante_mqtt.py — a major 2.x exige CallbackAPIVersion no construtor,
# assinatura usada em ao_conectar/ao_receber. Nao trocar por 1.x sem reescrever
# os callbacks. sqlite3 e json vem da biblioteca padrao.
paho-mqtt==2.1.0
